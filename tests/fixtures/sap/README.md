# Synthetic SAP transport fixtures

These payloads test envelope handling only. They are not sandbox recordings,
official metadata, schema evidence or live connectivity evidence (IR-01).
Error bodies and metadata XML in the tests are also deliberately synthetic.
